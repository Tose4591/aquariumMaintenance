<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="/images/logo/logo.png" alt="" height="100">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
            aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ms-auto me-5">
                <li class="nav-item">
                    <a class="nav-link <% if (page ==='/') { %> active" aria-current="page" <% }else{ %> "
                        <% } %> href=" /">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <% if (page ==='gallery') { %> active" aria-current="page" <% }else{ %> "
                        <% } %>
                        href=" /gallery">Gallery</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <% if (page ==='technical') { %> active" aria-current="page" <% }else{ %> "
                        <% } %>
                        href=" /technical">Technical</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link <% if (page ==='contact') { %> active" aria-current="page" <% }else{ %> "
                        <% } %>
                        href=" /contact">Contact</a>
                </li>
            </ul>
        </div>
    </div>
</nav>